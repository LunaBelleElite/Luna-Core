# Handoff status

Last-known computer and check-in time. Read/written by the Wake Up and Debrief protocols.

This is the `main` snapshot's placeholder copy. Real handoff content is working state: it stays on `dev` and never merges here. The file itself exists on `main` because `CLAUDE.md` and `scripts/validate-luna-core-setup.sh` both expect the path to be there, and because a project bootstrapped from this kit is given its own fresh copy.

- **Computer:** (none recorded — no Wake Up has run against this snapshot)
- **Last checked:** (never)
