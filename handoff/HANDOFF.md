# Handoff notes

Current state of this project, written so a person or AI with zero prior context could continue from here. Overwritten each time the Debrief Protocol runs.

This is the `main` snapshot's placeholder copy. Luna-Core's own handoff notes are working state and stay on `dev`; this file exists on `main` because `CLAUDE.md` and `scripts/validate-luna-core-setup.sh` both expect the path to be there, and because a project bootstrapped from this kit needs the path to already exist.

## What this project is

Luna-Core is a starter kit for Claude Code projects: four default agents, the Wake Up / Debrief session protocols, a baseline `CLAUDE.md`, a versioning scheme, and the scripts that install all of it into a new project. Start with `README.md`'s "Getting started" section.

## Where this project publishes

`origin` is `https://github.com/LunaBelleElite/Luna-Core.git`. "Publish" means an ordinary `git push` — no bundle, no synced-drive hub folder.

Recorded under this exact heading because the Wake Up protocol's first step looks for it by name — a clone carries no record of the address it came from.

## Current state

- This is `main`: the settled, stranger-facing snapshot. All development happens on `dev`.
- For this snapshot's onboarding-simulation status, see the caveat near the top of `README.md` — kept there rather than restated here, so there's exactly one place that claim can go stale.

## In progress / not yet resolved

- Nothing is tracked here. Open work lives on `dev`.

## Next steps

- Building a new project from this kit: follow `README.md`'s "Getting started".
- Working on Luna-Core itself: switch to `dev`, which carries the reference docs, the memory folder and the real handoff notes.
