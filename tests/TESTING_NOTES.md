# Testing notes hub

Read end to end by `luna-core-qa-tester` before it does any work. This file
is a **hub, not the notes** — the notes live in `tests/notes/`, and the table
below is the authority on which of them exist and when they apply.

| Notes file | Applies to | Read when |
| --- | --- | --- |
| `notes/live-checks.md` | Checks run against the real toolkit — bootstrap, validator, merge | Every pass |
| `notes/open-items.md` | Index of open questions, one row each | Every pass |
