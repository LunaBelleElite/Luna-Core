# Test index

Names every test in this project, so an agent can find the right one by
grepping this file instead of reading the test tree.

This is `main`'s placeholder copy. Luna-Core's own test index stays on
`dev`; a project bootstrapped from this kit gets its own fresh copy.

| Test | File | Covers |
| --- | --- | --- |
